import { prisma } from "@/lib/prisma";
export default async function SystemPage() { const [heartbeats, queued, failed] = await Promise.all([prisma.workerHeartbeat.findMany({ orderBy: { lastSeenAt: "desc" } }), prisma.socJob.count({ where: { status: "QUEUED", deletedAt: null } }), prisma.socJob.count({ where: { status: "FAILED", deletedAt: null } })]); return <section className="space-y-4"><h2 className="font-display text-xl font-bold">สถานะระบบ</h2><div className="grid gap-4 md:grid-cols-2"><div className="rounded-card border border-line bg-surface p-5"><div className="text-sm text-muted">SOC รอประมวลผล</div><div className="mt-2 text-3xl font-bold">{queued}</div></div><div className="rounded-card border border-line bg-surface p-5"><div className="text-sm text-muted">SOC ล้มเหลว</div><div className="mt-2 text-3xl font-bold">{failed}</div></div></div><div className="rounded-card border border-line bg-surface p-5"><h3 className="font-bold">Worker heartbeat</h3>{heartbeats.length ? heartbeats.map((h) => <div key={h.name} className="mt-3 border-t border-line pt-3 text-sm">{h.name}: {h.status} · ล่าสุด {h.lastSeenAt.toLocaleString("th-TH")}{h.lastError ? <div className="text-danger">{h.lastError}</div> : null}</div>) : <p className="mt-2 text-sm text-muted">ยังไม่ได้รับ heartbeat จาก worker</p>}</div></section>; }
